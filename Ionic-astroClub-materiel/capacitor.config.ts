import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.astroClubApp',
  appName: 'Ionic-astroClub-materiel',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
