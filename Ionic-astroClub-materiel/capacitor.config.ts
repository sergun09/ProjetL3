import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic-astroClub-materiel',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
